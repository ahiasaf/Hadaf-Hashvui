# הדף השבועי — הוראות לסוכן

**קרא לפני שאתה נוגע במשהו:**
`docs/handoff-session.md` — מצב הפרויקט, מה סוכם, ומה פתוח.
`docs/HANDOVER.md` — המסמך הטכני המלא.

---

## עם מי אתה עובד

**אחיאסף פנש**, מרכז התוכנית. **לא לכתוב "הרב".**

* **דחוף ישירות ל-`main`.** לא ענף, לא PR. זו הוראה מפורשת.
* **דבר על אפקט למשתמש, לא על קוד.**
* **נתח לפני שאתה מתכנת.** לרוב הוא רוצה לראות תוכנית ולהגיב.
* **בלי גרסאות שבורות.** האפליקציה כבר מופצת לצוות בשטח.
* **בלי להגזים בהגנות.** "האם אתה בטוח" מספיק.
* כתוב בעברית, כולל הודעות קומיט והערות קוד.

---

## כללים טכניים שאין לעבור עליהם

* **ES5 בלבד.** בלי `let`, בלי חיצים, בלי `class`. הטלפונים בשטח ישנים.
* **אין שלב בנייה ואין CDN.** `git push` הוא הפריסה (Vercel).
* **גם `apps-script.gs` נפרס מעצמו** — יש workflow שדוחף אותו
  לגוגל בכל שינוי. אין לומר לאחיאסף להעתיק אותו ידנית.
* **ארבע גרסאות חייבות להיות זהות** בכל שינוי:
  `APP_VERSION` (index.html) · `CACHE_NAME` (sw.js) ·
  `STUDIO_VER` (studio.html) · `DAF_REV` (data.js)
* **הרץ `python3 tools/preflight.py` לפני כל דחיפה.**
  הוא בודק גם שמות כפולים ומחלקות CSS יתומות — שתי בדיקות שנוספו
  אחרי תקלות בייצור.
* **`/admin` אינו מקושר משום מקום.** אל תוסיף קישור אליו.
* **אין טלפונים ואין שמות בריפו.** הריפו ציבורי.

## שתי מלכודות שחוזרות

* **`no-cors` תמיד "מצליח"** — התשובה אטומה. כל כתיבה לגיליון
  חייבת קריאה חוזרת שמאמתת.
* **אל תמחק CSS לפי טווח בין עוגנים.** זה כבר בלע שבעה כללים
  ושבר מסך בייצור. ערוך תוספתית.
* **קריאה שנכשלה אינה לשונית ריקה.** `rows.length < 2` ו-
  `if (Object.keys(m).length)` מיזגו את שניהם, ומספר שהתרוקן
  שרד לנצח — בארבעה קבצים. כישלון משאיר את המטמון, ריק מרוקן.

שאר המלכודות — בפרק 4 של `docs/handoff-session.md`.

---

## בדיקות

אין רץ בדיקות בריפו. כותבים תסריטי Playwright ב-scratchpad ומריצים
את הקוד האמיתי מול גיליון מדומה (`ctx.route`).
`docs.google.com` חסום מסביבת הפיתוח.

```
bash scratchpad/srv.sh       # שרת על 8899 משורש הריפו
node scratchpad/smoke.js     # כל המסכים, שתי רזולוציות
```
