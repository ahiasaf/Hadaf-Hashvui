/* ============================================================
   שלוש דרכי הלימוד — מקור אחד לכל המסכים.
   ============================================================
   "המסך הייצוגי אמור להיות שלושת הבאנרים הצבעוניים האלה."

   הן הופיעו בלוח המפורט בלבד, והתקציר שבאזור הניהול הראה
   במקומן שתי שורות אפורות של מספרים. אותה שאלה, שתי תשובות
   שנראות שונה — וזה בדיוק מה שנראה כמו תקלה.

   **האיורים אינם קישוט.** הם מה שמבדיל בין שלוש קטגוריות
   בסריקה של חצי שנייה, והם משאירים את המסך צעיר בלי לרדת
   לאמוג'י.

   **והכיתוב אינו כאן.** הוא ב-`WAYS_T` שב-data.js, ולכן
   נערך בניהול ככל נוסח אחר. המפתחות — 'אבות ובנים',
   'חבורת לימוד', 'לימוד עצמי' — הם מה שכתוב בגיליון, ואין
   לגעת בהם.
   ============================================================ */
var WAYS = [
  /* גדול וקטן, ורווח ביניהם — בלי הרווח הם נקראים ככתם אחד */
  { key:'אבות ובנים', t:'dad', c:'var(--gold)',
    art:'<circle cx="5.6" cy="5" r="3"/>' +
        '<path d="M1 17.5c0-3 2-4.8 4.6-4.8s4.6 1.8 4.6 4.8z"/>' +
        '<circle cx="15" cy="8.4" r="2.2"/>' +
        '<path d="M11.4 17.5c0-2.1 1.6-3.5 3.6-3.5s3.6 1.4 3.6 3.5z"/>' },
  /* שלושה בשורה עם כתף אחת משותפת — חבורה, לא נקודות */
  { key:'חבורת לימוד', t:'group', c:'var(--green-d)',
    art:'<circle cx="4.4" cy="6.4" r="2.3"/><circle cx="10" cy="5.6" r="2.6"/>' +
        '<circle cx="15.6" cy="6.4" r="2.3"/>' +
        '<path d="M.8 17.2c0-3 4.1-4.9 9.2-4.9s9.2 1.9 9.2 4.9z"/>' },
  /* פער בשדרה. בלעדיו שני חצאי הספר נוגעים, וב-26 פיקסלים הם
     נקראים כמלבן כהה ולא כספר פתוח. */
  { key:'לימוד עצמי', t:'solo', c:'var(--blue)',
    art:'<path d="M2.4 4.6c2.4-1.1 4.5-1.1 6.9 0v11.2c-2.4-1.1-4.5-1.1-6.9 0z"/>' +
        '<path d="M17.6 4.6c-2.4-1.1-4.5-1.1-6.9 0v11.2c2.4-1.1 4.5-1.1 6.9 0z"/>' }
];

function WayLbl(key) {
  for (var i = 0; i < WAYS.length; i++) {
    if (WAYS[i].key !== key) continue;
    return (window.WAYS_T && WAYS_T[WAYS[i].t]) || WAYS[i].key;
  }
  return key || ((window.WAYS_T && WAYS_T.none) || '');
}

/* המשבצות. `count(key)` מחזיר מספר; `opt.go(key)` — כשיש לאן
   ללחוץ. בלי `go` אלה שלוש תצוגות, וזה המצב בתקציר. */
function WaysTiles(count, opt) {
  opt = opt || {};
  return '<div class="tiles">' + WAYS.map(function (g) {
    var n = count(g.key) || 0;
    var live = !!(opt.go && n);
    var tag = live ? 'button' : 'div';
    return '<' + tag + ' class="tile' + (live ? ' go' : '') +
      (opt.sel === g.key ? ' sel' : '') + '" style="--c:' + g.c + '"' +
      (live ? ' onclick="' + opt.go + '(\'' + g.key + '\')"' : '') + '>' +
      '<svg viewBox="0 0 20 20" aria-hidden="true">' + g.art + '</svg>' +
      '<b>' + n + '</b><span>' + WayEsc(WayLbl(g.key)) + '</span>' +
      (live ? '<i class="chev">' + (opt.sel === g.key ? '×' : '←') + '</i>' : '') +
      '</' + tag + '>';
  }).join('') + '</div>';
}

function WayEsc(t) {
  return String(t == null ? '' : t).replace(/[&<>"]/g, function (c) {
    return { '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;' }[c];
  });
}
